# R10.2 — Live Active Desktop Qualification

**Booking:** `IICPXRD [A]202600041` (pk 367)  
**Updated:** 2026-08-11  
**Status:** **BLOCKED — RAVI WorkstationRdpSecret missing (Windows credentials)**

## Progress from operator Start Analysis

Screenshot `/analysis-launch/367` showed:
- Data Workspace paths **visible** (R10.2 path payload working)
- Progress stuck at **49% Synchronizing input data**

### Live diagnosis (run 31485186545)

| Step | Result |
|------|--------|
| Check-in accepted | **PASS** |
| Session created | **PASS** |
| PREPARE_WORKSTATION | **PASS** (COMPLETED; Downloaded 2 file(s)) |
| Input sync on RAVI | **PASS** |
| Guacamole provision | **FAIL** |
| ACTIVE desktop | **FAIL** |

### FIRST FAILURE / ROOT CAUSE

```
Workstation Windows credentials are not configured.
Re-run the Remote Analysis Agent installer
(or set Workstation RDP Secret in Django Admin)
so automatic login can succeed.
```

Multiple sessions failed with this exact `failure_detail` after prepare completed.
Latest session then stuck PREPARING with a PENDING prepare while the SPA kept re-polling launch.

**This is configuration, not scheduler/allocator.** Guacamole refuses to create an RDP connection without `WorkstationRdpSecret` username + encrypted password.

## Operator action required (credentials)

Choose **one**:

### A. Re-run RAA installer on RAVI (preferred)
1. Run Remote Analysis Agent installer on RAVI
2. Enter valid **Windows RDP / Administrator password** when prompted
3. Complete equipment link for PXRD [A]
4. Confirm service is Running
5. Retry Start Analysis for booking 367

### B. Django Admin
1. Admin → Analysis Workstation → **RAVI**
2. Set **Workstation RDP Secret** (username + password)
3. Save (password is encrypted at rest)
4. Retry launch

**Do not invent credentials.** Use the real Windows account password for the Analysis PC.

## Code fixes after diagnosis

- Fail fast before PREPARE if RDP credentials missing (`rdp_credentials_missing`)
- Launch API returns structured `failure.user_message`
- Frontend stops polling on credential failure
- Diagnostic workflow: Diagnose RAVI RDP Secret Presence
- Deploy tag: `v2.5.14-ra-r10-2-rdp-preflight`

## Qualification matrix (updated)

| # | Item | Status |
|---|------|--------|
| 1–8 | Mapping → allocation → check-in → prepare workspace | **PASS** |
| 9 | Input synchronization | **PASS** (agent downloaded files) |
| 10 | Session creation | **PASS** |
| 11–15 | Token → Guacamole → ACTIVE + interaction | **FAIL / BLOCKED** (no RDP secret) |
| 16 | Data Workspace visibility | **PASS** (paths shown in UI) |
| 17–26 | Timer / end / multi-PC / security… | **NOT TESTED** |

**R10.2 remains BLOCKED until RDP credentials are configured and ACTIVE Guacamole is confirmed.**
