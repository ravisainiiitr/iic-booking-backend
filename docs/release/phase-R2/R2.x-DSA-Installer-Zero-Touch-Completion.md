# R.2.x — DSA Installer Zero-Touch Completion

**Status:** Fixed  
**Related:** R.2.1–R.2.3, DSA installer  
**Detail:** [DepartmentSyncAgent docs/R2.x-Zero-Touch-Installer-Completion.md](../../../../DepartmentSyncAgent/docs/R2.x-Zero-Touch-Installer-Completion.md)

## Summary

Removed remaining legacy Enrollment Secret / Agent UUID dependencies from the **standard** DSA Setup path.

| Fix | Detail |
|-----|--------|
| UI | Enrollment Secret / Agent UUID only under **Advanced → Recovery → Legacy Enrollment** |
| Equipment list | Claim embeds `equipment_tree`; else `GET /api/v1/provisioning/dsa/equipment-tree/` with Bearer / `X-Agent-Access-Token` / portal Token |
| Legacy API | Still available for break-glass; also accepts Bearer without secret |
| Department UI | Single department auto-selected and hidden |

## Tests

`iic_booking/device_provisioning/tests/test_dsa_zero_touch_equipment_r2x.py`  
`DepartmentSyncAgent.Installer.Tests` (5 passed)
