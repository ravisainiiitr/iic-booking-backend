# R14.1 — Production Deployment

Status values: **PASS / PARTIAL / BLOCKED / NOT TESTED**.

Date: 2026-08-14

## Verdict

**NOT TESTED** — merge, tag, migrate, and deploy were not started. Live authenticated E2E did not pass, so production was left on the current R13 release.

## Current production (unchanged)

| Item | Value |
| --- | --- |
| Portal | `https://equip.iitr.ac.in` |
| Backend tag on origin | `v2.5.40-r13-ghost-reserved` |
| Backend `origin/master` | `f8c0892` (`Merge PR #84 hotfix/r13-session-heartbeat-stall`) |
| Frontend `origin/master` | `83d38f7` (`Merge PR #18 hotfix/r13-require-data-selection`) |
| Health ready | HTTP 200 `ready` (2026-08-14T05:07:59Z) |
| Health live | HTTP 200 `ok` |
| Auto Complete equipment enabled | **none** (feature not deployed; field does not exist in production until 0188) |

## Intended next version (not created)

`v2.5.41-r14-auto-complete-ra-data-selection`

Do not invent a conflicting tag. Confirm `git tag` on origin immediately before creating it.

## What would be deployed (when gates pass)

### Backend

1. Merge `feature/r14-auto-complete-ra-data-selection` into `master` (R14 files only).
2. Tag `v2.5.41-r14-auto-complete-ra-data-selection`.
3. Run **Backend Release** then **Deploy Backend** (`.github/workflows/backend-deploy.yml`) with that tag. No force push.
4. Run **Migrate Production** (`.github/workflows/migrate-production.yml`) so `0188_equipment_auto_complete_and_data_selection` applies through `manage.py migrate --noinput`.
5. Confirm `showmigrations equipment` includes `[X] 0188_equipment_auto_complete_and_data_selection`.
6. Re-check `/api/v1/analysis/health/ready/` and `/live/`.

Do **not** merge `feature/r11-raa-identity-software-catalog`.

### Frontend

1. Merge `feature/r14-auto-complete-ra-data-selection` (`c5c1753`) into frontend `master`.
2. Existing `deploy.yml` on master push builds/deploys the SPA.
3. Smoke: login → booking details → Open Analysis Workspace → Current / Previous / Upload cards.

### Auto Complete enablement after deploy

Default remains **OFF**. Do not enable globally.

Approved equipment for ON: **none identified in this session**. Leave all OFF until an operator names specific equipment.

## What was not done

| Step | Result |
| --- | --- |
| Merge backend | NOT TESTED |
| Merge frontend | NOT TESTED |
| Tag | NOT TESTED |
| Production migrate 0188 | NOT TESTED |
| Backend deploy | NOT TESTED |
| Frontend deploy | NOT TESTED |
| Production authenticated smoke | BLOCKED |
| Enable Auto Complete on named equipment | NOT TESTED (none approved) |

## Safety

- No production DB edits
- No test users added
- No force push
- No debug settings or development API URL introduced for production builds (frontend still falls back to `127.0.0.1:8000` only when no runtime/`VITE_API_URL` is set; production uses runtime config / `/api`)
