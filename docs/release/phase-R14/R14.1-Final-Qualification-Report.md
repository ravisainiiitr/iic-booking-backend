# R14.1 — Final Qualification Report

Status values below are only **PASS / PARTIAL / BLOCKED / NOT TESTED**. PASS is used only when the check was actually executed.

Date: 2026-08-14

## Verdict

**PARTIAL** — R14 was rebased onto production `master`, unit/build gates passed, and production was left unchanged. Live authenticated E2E, merge, tag, migrate, and deploy were **not** executed because the live E2E gate is blocked.

Do not treat code existence as PASS.

## Why the original R14 SHAs were not merged

| Repo | Original branch | Original SHA | Problem |
| --- | --- | --- | --- |
| Backend | `feature/r11-raa-identity-software-catalog` | `1d3a606` | Stale vs production `master` (`f8c0892`). Production `0187` is already PI pricing. Merging this branch would fight R12/R13/PI. |
| Frontend | `feature/r13-allocation-ux-data-first` | `d41cb34` | Cherry-picked onto `origin/master` instead of merging the whole R13 feature branch. |

## Qualified branches (this session)

| Repo | Branch | Base | SHA |
| --- | --- | --- | --- |
| Backend | `feature/r14-auto-complete-ra-data-selection` | `origin/master` `f8c0892` | recorded after commit (see git log on this branch) |
| Frontend | `feature/r14-auto-complete-ra-data-selection` | `origin/master` `83d38f7` | `c5c1753` (`feat(R14): data-first analysis workspace and simplified data browser`) |

No R14.1 pull requests were opened. No tags were created. No force push.

## Migration

Production-compatible migration is:

`iic_booking/equipment/migrations/0188_equipment_auto_complete_and_data_selection.py`

- Depends on `0187_equipment_pi_and_pi_charge_profile`
- Adds `Equipment.auto_complete_booking` (Boolean, default `False`)
- Adds `Booking.analysis_data_selection` (JSONField, default `dict`)
- Additive only; default keeps Auto Complete **OFF** for all existing equipment

`makemigrations equipment --check --dry-run` did **not** try to recreate those fields. It did report unrelated pre-existing status-field drift that would generate `0189_alter_booking_disruption_kind_…`. That drift was **not** generated and is **not** part of R14.

Full `migrate` on a fresh Postgres test DB hit a pre-existing duplicate index in `remote_analysis` (`remote_anal_status_7b334f_idx`). That is unrelated to 0188. Unit tests were therefore run with `--nomigrations` against current models (same approach used for R12 qualification).

## Tests actually run

| Suite | Result | Evidence |
| --- | --- | --- |
| R14 auto-complete + R14 data browser + R7 completion + R12 data browser + RA integration smoke + R11 allocation | **PASS** | Docker `iic_booking_local_django`, SQLite, `--nomigrations --create-db`: **38 passed** |
| Django `manage.py check` | **PASS** | `System check identified no issues (0 silenced)` |
| Frontend Vitest `src/components/analysis/dataBrowserUtils.test.ts` | **PASS** | `npx vitest run` — **3 passed** (v4.1.10) |
| Frontend `npm run build` | **PASS** | vite 7.3.0, built in 19.55s |
| Full migrate on Postgres | **BLOCKED** | Pre-existing `remote_anal_status_7b334f_idx` duplicate; not an R14 schema change |
| `makemigrations equipment --check` | **PARTIAL** | 0188 matches models; unrelated status-field drift would create 0189 (not committed) |

### Auto-complete matrix (unit)

| # | Case | Result |
| --- | --- | --- |
| 1 | `auto_complete_booking=False`, ended, result exists | PASS (`test_auto_complete_disabled_equipment_never_completes`) |
| 2 | Flag true, booking not ended | PASS (`test_auto_complete_skips_when_end_time_not_reached`) |
| 3 | Flag true, ended, no Active workspace | PASS (`test_auto_complete_skips_missing_workspace_and_hidden_only`) |
| 4 | Flag true, ended, workspace, no meaningful files | PASS (`test_auto_complete_skips_when_no_result_data` / empty / hidden) |
| 5 | Flag true, ended, workspace, valid result | PASS (`test_auto_complete_completes_when_result_data_exists`) |
| 6 | Empty result directory | PASS (`test_auto_complete_skips_empty_result_directory`) |
| 7 | Hidden file only | PASS (same missing-workspace/hidden test) |
| 8 | Temporary file only | PASS (`scratch.tmp` in `test_auto_complete_skips_when_no_result_data`) |
| 9 | ACTIVE Remote Analysis session | PASS (`test_auto_complete_skips_active_raa_session`; session remains ACTIVE) |
| 10 | Manually completed, then auto-complete | PASS (`test_manual_then_auto_complete_is_safe`) |
| 11 | Task executes twice | PASS (`test_auto_complete_twice_is_idempotent` + R7 idempotent) |
| 12 | Simultaneous manual + auto | PARTIAL | Sequential manual-then-auto + `select_for_update`; true concurrent threads not executed |

Completion goes through `booking_completion_service.try_auto_complete_booking` (Celery task does not set status directly). Email path attaches `[]`.

### Data-selection authorization (API unit)

| Case | Result |
| --- | --- |
| Unauthenticated browse | PASS 401/403 |
| Other user browse/select | PASS 403 |
| Other user's booking as source | PASS 403 |
| Same user, other equipment as source | PASS 403 |
| Previous scope same equipment only | PASS |
| Search by virtual booking ID | PASS |
| Selection persisted before allocation | PASS (`analysis_reservation_id is None`) |

## Implementation reuse (no duplicate engines)

- Auto-complete: existing Celery task name `equipment.auto_complete_bookings_with_data_after_end` now delegates to `booking_completion_service`
- Result detection: existing `has_material_result_files` (hidden/temp/control files skipped)
- Data browser: existing R12 `BookingAnalysisDataBrowserService` (enhanced, not replaced)
- Allocation: existing RAA engine (frontend still does not pick a PC)
- Upload: existing `upload_past_data` / TransferManager; selection JSON is updated after upload

## Live E2E / production

See [R14.1-Live-E2E-Report.md](R14.1-Live-E2E-Report.md) and [R14.1-Production-Deployment.md](R14.1-Production-Deployment.md).

- Authenticated live E2E: **BLOCKED** (no controlled test account in this session)
- Merge / tag / migrate / deploy: **NOT TESTED** (gated on live E2E)
- Auto Complete on production equipment: **left OFF** (not deployed; default remains False)

## Known limitations

1. Production migrate of 0188 has not been applied.
2. Full Django migrate on a clean Postgres DB is still blocked by a pre-existing remote_analysis index collision; production already has those migrations applied.
3. True simultaneous manual+auto completion was not thread-tested.
4. RAA READY/ACTIVE desktop, session-end S3 capture, and RAA disk cleanup were not live-tested.
5. Frontend Vitest is not a package.json script; the three tests were run via `npx vitest run`.
