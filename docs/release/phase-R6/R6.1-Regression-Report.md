# R6.1 — Regression Report

## Must keep working

| Scenario | Expected |
|----------|----------|
| Existing Analyze Data booking launch | Still auto-allocates; optional software selection |
| Agent heartbeat / register | Unchanged |
| Inventory full POST (old agents) | Treated as `syncMode=full` upsert |
| Installer catalog GET + inventory POST | Unchanged paths |
| Queue when no PC free | Unchanged |
| Workflow multi-software requirements | Unchanged |
| Django Admin catalog | Still works alongside SPA |
| Lab consumable `/api/inventory` | Untouched |

## New smoke checks

1. Create catalog entry in SPA → appears in mapping matrix + installer catalog (active)
2. Map equipment ↔ software → Workspace shows options with description/usage/files
3. Allocate with logging → `scheduler_decision` lines in portal logs
4. Agent inventory → titles from PF/Start Menu appear; second sync can be delta
5. Archive software → hidden from active matrix; mappings retained

## Known non-regressions

- IP still omitted from workstation serializer
- Researcher APIs still hide hostname
