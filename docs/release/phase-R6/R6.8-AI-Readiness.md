# R6.8 — AI Readiness

**Metadata only. Do not build an AI recommender in R6.1.**

| Field | Location |
|-------|----------|
| `ai_tags`, `ai_metadata` | `AnalysisSoftwareCatalog` |
| Workflow `ai_*` fields | `workflow_models.py` (existing) |

Workspace may **display** tags; no recommendation engine, no auto-pick software/workflow via LLM.
