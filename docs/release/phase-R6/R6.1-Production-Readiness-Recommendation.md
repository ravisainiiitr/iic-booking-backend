# R6.1 — Production Readiness Recommendation

## Recommendation: **GO with conditions**

R6.1 closes the operator-facing gaps (Catalog, Mapping, Inventory) and hardens discovery/sync/scheduler transparency without introducing parallel models.

### Conditions before production traffic

1. Run migration `0023` on staging; smoke catalog + matrix + Analyze Data
2. Deploy new agent to at least one Analysis PC; confirm inventory count and delta logs
3. Confirm `remote_analysis.scheduler` log level is INFO in ops logging stack
4. Train admins on SPA paths (`/remote-analysis/software-catalog`, `/equipment-software`) vs Django Admin
5. Leave Network License Server deep integration deferred — configure `license_server_url` + seats only if needed

### Not blocking

- AI recommender
- GPU util metrics
- Shared Discovery NuGet between Agent and Installer

### Rollback

- Frontend: remove SPA routes (Django Admin remains)
- Backend: APIs additive; disable routes via feature flag if needed (or leave unused)
- Agent: prior registry-only build still compatible with full inventory POST
- Migration `0023` fields are additive (safe to keep)
